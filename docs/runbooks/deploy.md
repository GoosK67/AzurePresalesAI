
# Deploy Runbook

- DEV: use `Deploy DEV` workflow (manual) with RG + location.
- PROD: use `Deploy PROD` workflow (requires approval in environment settings).
- Local: `infra/azure-ai-platform/deploy.sh` or `deploy.ps1`.

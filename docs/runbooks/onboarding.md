
# Onboarding

1. Request access to Azure subscription + RG.
2. Configure GitHub OIDC (see `scripts/configure-oidc-azure.ps1`).
3. Set repo secrets: `AZURE_TENANT_ID`, `AZURE_SUBSCRIPTION_ID`, `AZURE_CLIENT_ID`.
4. Fork/clone repo and open a feature branch.


# Workshop 1 – Networking & DNS (Hands-on)

## Goals
- Understand PE subnet policies
- Deploy VNet + subnets + Private DNS zones

## Steps
1. Deploy `network.bicep` and `privateDns.bicep` via `platform.bicep`.
2. Verify DNS links and records using `nslookup` for `privatelink.*`.
3. Q&A and pitfalls.
